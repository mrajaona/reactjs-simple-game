import variables from "./consts.module.scss";

export const GAMEBOX_HEIGHT = parseInt(variables["gamebox-height"]);
export const GAMEBOX_WIDTH = parseInt(variables["gamebox-width"]);

export const BIRD_SIZE = parseInt(variables["bird-size"]);
